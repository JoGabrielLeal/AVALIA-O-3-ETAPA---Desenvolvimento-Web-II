# Projeto de Rede Social
### Considerações iniciais:
Para este projeto, eu me inspirei no framework [Laravel](https://laravel.com/).
Peguei algumas ideias, como o `autoload.php` (para carregar classes de forma automática) e o `bootstrap.php` (inicializar alguns arquivos padrão do sistema).
Utilizei uma estrutura de arquivos de configuração.
E utilizei de alguns padrões de projeto, mas nada complexo.
